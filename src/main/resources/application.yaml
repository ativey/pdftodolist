spring:
  mvc:
    view.prefix: /WEB-INF/jsp/
    view.suffix: .jsp

  datasource:
    url: jdbc:postgresql://localhost:5432/pdftodolist
    username: art
    password: XXX

  jpa:
    show-sql: false
    properties.hibernate.dialect: org.hibernate.dialect.PostgreSQL94Dialect

    hibernate:
      ddl-auto: create
      #ddl-auto = update
      naming-strategy: org.hibernate.cfg.ImprovedNamingStrategy

