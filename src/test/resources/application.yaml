spring:
    mvc:
        view.prefix: /WEB-INF/jsp/
        view.suffix: .jsp

    jpa:
        show-sql: false

        hibernate:
            #ddl-auto: create
            ddl-auto: update
            naming-strategy: org.hibernate.cfg.ImprovedNamingStrategy

